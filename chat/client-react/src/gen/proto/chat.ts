// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v5.27.3
// source: chat.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "examples.chat";

export interface SendMessageRequest {
  room: string;
  user: string;
  text: string;
}

export interface SendMessageResponse {
  /** "ok" */
  status: string;
}

export interface JoinRoomRequest {
  room: string;
  user: string;
}

export interface JoinRoomResponse {
  status: string;
}

/** Event pushed to websocket subscribers. */
export interface MessageEvent {
  room: string;
  user: string;
  text: string;
  tsUnixMs: number;
}

/** Presence updates (JOIN / LEAVE) pushed separately from chat text. */
export interface PresenceEvent {
  room: string;
  user: string;
  action: PresenceEvent_Action;
  tsUnixMs: number;
}

export enum PresenceEvent_Action {
  ACTION_UNKNOWN = 0,
  JOIN = 1,
  LEAVE = 2,
  UNRECOGNIZED = -1,
}

export function presenceEvent_ActionFromJSON(object: any): PresenceEvent_Action {
  switch (object) {
    case 0:
    case "ACTION_UNKNOWN":
      return PresenceEvent_Action.ACTION_UNKNOWN;
    case 1:
    case "JOIN":
      return PresenceEvent_Action.JOIN;
    case 2:
    case "LEAVE":
      return PresenceEvent_Action.LEAVE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PresenceEvent_Action.UNRECOGNIZED;
  }
}

export function presenceEvent_ActionToJSON(object: PresenceEvent_Action): string {
  switch (object) {
    case PresenceEvent_Action.ACTION_UNKNOWN:
      return "ACTION_UNKNOWN";
    case PresenceEvent_Action.JOIN:
      return "JOIN";
    case PresenceEvent_Action.LEAVE:
      return "LEAVE";
    case PresenceEvent_Action.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseSendMessageRequest(): SendMessageRequest {
  return { room: "", user: "", text: "" };
}

export const SendMessageRequest: MessageFns<SendMessageRequest> = {
  encode(message: SendMessageRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.room !== "") {
      writer.uint32(10).string(message.room);
    }
    if (message.user !== "") {
      writer.uint32(18).string(message.user);
    }
    if (message.text !== "") {
      writer.uint32(26).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendMessageRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.room = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.user = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendMessageRequest {
    return {
      room: isSet(object.room) ? globalThis.String(object.room) : "",
      user: isSet(object.user) ? globalThis.String(object.user) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
    };
  },

  toJSON(message: SendMessageRequest): unknown {
    const obj: any = {};
    if (message.room !== "") {
      obj.room = message.room;
    }
    if (message.user !== "") {
      obj.user = message.user;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create(base?: DeepPartial<SendMessageRequest>): SendMessageRequest {
    return SendMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SendMessageRequest>): SendMessageRequest {
    const message = createBaseSendMessageRequest();
    message.room = object.room ?? "";
    message.user = object.user ?? "";
    message.text = object.text ?? "";
    return message;
  },
};

function createBaseSendMessageResponse(): SendMessageResponse {
  return { status: "" };
}

export const SendMessageResponse: MessageFns<SendMessageResponse> = {
  encode(message: SendMessageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendMessageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendMessageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendMessageResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: SendMessageResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create(base?: DeepPartial<SendMessageResponse>): SendMessageResponse {
    return SendMessageResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SendMessageResponse>): SendMessageResponse {
    const message = createBaseSendMessageResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseJoinRoomRequest(): JoinRoomRequest {
  return { room: "", user: "" };
}

export const JoinRoomRequest: MessageFns<JoinRoomRequest> = {
  encode(message: JoinRoomRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.room !== "") {
      writer.uint32(10).string(message.room);
    }
    if (message.user !== "") {
      writer.uint32(18).string(message.user);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JoinRoomRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRoomRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.room = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.user = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRoomRequest {
    return {
      room: isSet(object.room) ? globalThis.String(object.room) : "",
      user: isSet(object.user) ? globalThis.String(object.user) : "",
    };
  },

  toJSON(message: JoinRoomRequest): unknown {
    const obj: any = {};
    if (message.room !== "") {
      obj.room = message.room;
    }
    if (message.user !== "") {
      obj.user = message.user;
    }
    return obj;
  },

  create(base?: DeepPartial<JoinRoomRequest>): JoinRoomRequest {
    return JoinRoomRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JoinRoomRequest>): JoinRoomRequest {
    const message = createBaseJoinRoomRequest();
    message.room = object.room ?? "";
    message.user = object.user ?? "";
    return message;
  },
};

function createBaseJoinRoomResponse(): JoinRoomResponse {
  return { status: "" };
}

export const JoinRoomResponse: MessageFns<JoinRoomResponse> = {
  encode(message: JoinRoomResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JoinRoomResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRoomResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRoomResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: JoinRoomResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create(base?: DeepPartial<JoinRoomResponse>): JoinRoomResponse {
    return JoinRoomResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JoinRoomResponse>): JoinRoomResponse {
    const message = createBaseJoinRoomResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseMessageEvent(): MessageEvent {
  return { room: "", user: "", text: "", tsUnixMs: 0 };
}

export const MessageEvent: MessageFns<MessageEvent> = {
  encode(message: MessageEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.room !== "") {
      writer.uint32(10).string(message.room);
    }
    if (message.user !== "") {
      writer.uint32(18).string(message.user);
    }
    if (message.text !== "") {
      writer.uint32(26).string(message.text);
    }
    if (message.tsUnixMs !== 0) {
      writer.uint32(32).int64(message.tsUnixMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.room = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.user = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.tsUnixMs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageEvent {
    return {
      room: isSet(object.room) ? globalThis.String(object.room) : "",
      user: isSet(object.user) ? globalThis.String(object.user) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      tsUnixMs: isSet(object.tsUnixMs) ? globalThis.Number(object.tsUnixMs) : 0,
    };
  },

  toJSON(message: MessageEvent): unknown {
    const obj: any = {};
    if (message.room !== "") {
      obj.room = message.room;
    }
    if (message.user !== "") {
      obj.user = message.user;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.tsUnixMs !== 0) {
      obj.tsUnixMs = Math.round(message.tsUnixMs);
    }
    return obj;
  },

  create(base?: DeepPartial<MessageEvent>): MessageEvent {
    return MessageEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MessageEvent>): MessageEvent {
    const message = createBaseMessageEvent();
    message.room = object.room ?? "";
    message.user = object.user ?? "";
    message.text = object.text ?? "";
    message.tsUnixMs = object.tsUnixMs ?? 0;
    return message;
  },
};

function createBasePresenceEvent(): PresenceEvent {
  return { room: "", user: "", action: 0, tsUnixMs: 0 };
}

export const PresenceEvent: MessageFns<PresenceEvent> = {
  encode(message: PresenceEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.room !== "") {
      writer.uint32(10).string(message.room);
    }
    if (message.user !== "") {
      writer.uint32(18).string(message.user);
    }
    if (message.action !== 0) {
      writer.uint32(24).int32(message.action);
    }
    if (message.tsUnixMs !== 0) {
      writer.uint32(32).int64(message.tsUnixMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PresenceEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePresenceEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.room = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.user = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.action = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.tsUnixMs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PresenceEvent {
    return {
      room: isSet(object.room) ? globalThis.String(object.room) : "",
      user: isSet(object.user) ? globalThis.String(object.user) : "",
      action: isSet(object.action) ? presenceEvent_ActionFromJSON(object.action) : 0,
      tsUnixMs: isSet(object.tsUnixMs) ? globalThis.Number(object.tsUnixMs) : 0,
    };
  },

  toJSON(message: PresenceEvent): unknown {
    const obj: any = {};
    if (message.room !== "") {
      obj.room = message.room;
    }
    if (message.user !== "") {
      obj.user = message.user;
    }
    if (message.action !== 0) {
      obj.action = presenceEvent_ActionToJSON(message.action);
    }
    if (message.tsUnixMs !== 0) {
      obj.tsUnixMs = Math.round(message.tsUnixMs);
    }
    return obj;
  },

  create(base?: DeepPartial<PresenceEvent>): PresenceEvent {
    return PresenceEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PresenceEvent>): PresenceEvent {
    const message = createBasePresenceEvent();
    message.room = object.room ?? "";
    message.user = object.user ?? "";
    message.action = object.action ?? 0;
    message.tsUnixMs = object.tsUnixMs ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
